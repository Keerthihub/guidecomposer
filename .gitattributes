# Keep source and documentation identical on every checkout. ExtendScript and
# shell files are especially sensitive to automatic CRLF conversion.
* text=auto eol=lf

*.png binary
*.p12 binary
*.zxp binary
